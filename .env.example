# ビルド時にJSON取得先を上書きする場合だけ設定する。
# 未指定時: 開発 ./testdata/melody、production /data/dictation/melody
# harmony指定時は未指定なら /data/dictation/harmony を使う。
# この値が /melody で終わる場合、harmonyでは末尾を /harmony に置き換える。
VITE_DATA_BASE_URL=/data/dictation/melody
