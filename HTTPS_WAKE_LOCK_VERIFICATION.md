# Screen Wake LockのHTTPS実機確認

Screen Wake Lock APIはsecure contextが必要なため、
`http://192.168...` のLANプレビューでは動作確認できない。

## 1. HTTPSへ配置するbuildを作る

検証用sample1を含むproduction buildを作る。

```powershell
npm run build:lan
```

生成された `dist/` の内容を、iPhoneからアクセスできるHTTPS対応の
静的ホスティングへ配置する。証明書はiPhoneが信頼できるものを使う。

配置後、次のURLでsample1が読み込めることを確認する。

```text
https://<検証ホスト>/?id=sample1
```

## 2. Wake Lockを確認する

1. iOS Safari 16.4以降で検証URLを開く。
2. 端末の自動ロック時間を短く設定する。
3. 試験モードへ切り替えて試験を開始する。
4. 自動ロック時間を超えても画面が消灯せず、sequenceが継続することを確認する。
5. 試験終了または試験中止後は通常の自動ロックへ戻ることを確認する。

## 3. 非表示時の安全な中止を確認する

1. 試験を開始する。
2. Safariを別タブへ切り替えるか、ホーム画面へ移動する。
3. 検証画面へ戻る。
4. 「画面が非表示になったため、試験と発音を中止しました」と表示されることを確認する。
5. 音声とsequenceが自動再開しないことを確認する。
6. 試験開始ボタンから新しい試験を明示的に開始できることを確認する。

バックグラウンド再生はサポートしない。Wake Lock非対応・取得失敗時は、
端末の自動ロックを一時的に無効化して画面を表示したまま使用する。
